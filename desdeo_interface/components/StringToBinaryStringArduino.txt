    String json;
    serializeJson(doc, json);
    const int k = json.length();
    byte jsonB[k * 8 + 4];
    for (int i = 0; i < k; i++) {
      char aChar = json.charAt(i);

      for (int j=7; j >= 0; j--) {
        byte b = bitRead(aChar, j);
        jsonB[i*j] = b;
      }
    }
    jsonB[k*8] = '1';
    jsonB[k*8] = '0';
    jsonB[k*8] = '0';
    jsonB[k*8] = '1';
    for (int i = 0; i < k*8+4; i++) {
      Serial.print(jsonB[i], BIN);
    }
    Serial.println();